import React from 'react';
import { Container, Typography } from '@mui/material';

export default function App() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>
        GBP Management Dashboard
      </Typography>
      <p>React frontend placeholder</p>
    </Container>
  );
}
